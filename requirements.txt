# Ralph Ollama Runner dependencies
# Using OpenAI-compatible API approach for Ollama

# OpenAI client library (supports Ollama via base_url parameter)
openai>=1.0.0

# Environment variable management
python-dotenv>=1.0.0

# HTTP requests (for health check)
requests>=2.28.0
